import 'package:flutter/material.dart';

class SheetBox extends StatelessWidget {
  const SheetBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.transparent,
      height: 400,
    );
  }
}
